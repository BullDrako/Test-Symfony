{% block body %}
    {# Comentario #}
    <h3>Resultado</h3>
    Escogido {{ operador }}: {{ num1 }} {{ simbolo }} {{ num2 }} = {{ resultado }}.<br/>
    Reiniciar: <a href="{{ URL }}">{{ nombreURL }}</a>
{% endblock %}